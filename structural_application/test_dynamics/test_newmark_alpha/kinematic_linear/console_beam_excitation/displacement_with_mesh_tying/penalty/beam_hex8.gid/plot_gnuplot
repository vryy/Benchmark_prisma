set terminal pdf enhanced color

#set size ratio -1
#set key outside
#set size 2.0, 1.5

#set size square
#set key left bottom

set output "output.pdf"
set xlabel "t [s]"
set ylabel "u [m]"
#set xtics 0.5
#set ytics 1.0
set title "displacements"
plot "displacement.txt" using ($1):($2) with lines title "excitation", \
     "displacement.txt" using ($1):($3) with linespoints ps 0.15 title "tip displacement (w/ mesh tying)", \
     "../../../displacement/beam_hex8.gid/displacement.txt" using ($1):($3) with lines title "tip displacement (wo/ mesh tying)", \
